"""de_toolkit — turn data-engineering study notes into a linked Obsidian vault.

The toolkit reads a small JSON ``Catalog`` of concepts (``data/content.json``)
and writes one interconnected Markdown note per concept into an Obsidian vault,
with YAML frontmatter, wikilinks, Maps-of-Content and a ``Home.md`` entry point.
"""

__version__ = "0.1.0"
